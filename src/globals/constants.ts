
export namespace Menu{
    export enum Items {
        Home,
        Rooms,
        Devices,
        Statistics,
        Routines,
        Settings,
    }

    export const DefaultItem: number = Items.Devices; // Select Device menu
}
